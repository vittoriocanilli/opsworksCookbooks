<?php defined('SYSPATH') or die('No direct script access.');

$scalarium['revision'] = <%= "#{@revision}".inspect %>;
$scalarium['log_dir'] = <%= "#{@log_dir}".inspect %>;
$scalarium['cache_dir'] = <%= "#{@cache_dir}".inspect %>;
$scalarium['deploy_root'] = <%= "#{@deploy_root}".inspect %>;
$scalarium['release_path'] = <%= "#{@release_path}".inspect %>;
$scalarium['memcaches'] = array(<%= "#{@memcache_server_ips}".inspect %>);
$scalarium['memcaches_sessions'] = array(<%= "#{@memcache_sessions_server_ips}".inspect %>);
?>